# ============================================================================
# litho-neural-operator-lab :: requirements
# ----------------------------------------------------------------------------
# Python 3.10+. PyTorch is required; pick the CUDA wheel that matches your
# driver / GPU compute capability. See https://pytorch.org/get-started/locally/
# for the appropriate index URL. If a "no kernel image" error surfaces at
# runtime, the installed wheel was built for an older compute capability —
# install a newer cuXX wheel.
#
# Install order:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install --upgrade pip wheel setuptools
#   pip install torch torchvision  # add --index-url ... if needed
#   pip install -r requirements.txt
# ============================================================================

# --- core numerical / DL ---------------------------------------------------
numpy>=1.26
scipy>=1.13
torch>=2.7            # picks up most recent compute-capability targets
torchvision>=0.22

# --- visualization ---------------------------------------------------------
matplotlib>=3.8

# --- data IO ---------------------------------------------------------------
h5py>=3.10
pyyaml>=6.0
pandas>=2.2

# --- experiment config / logging ------------------------------------------
hydra-core>=1.3
omegaconf>=2.3
tqdm>=4.66

# --- notebooks (Phase 1~2 explore, final demo) -----------------------------
jupyterlab>=4.0
ipykernel>=6.29

# --- testing ---------------------------------------------------------------
pytest>=8.0

# --- optional :: neural operator reference impl ----------------------------
# Uncomment if you want to compare against the open-source FNO reference
# instead of the self-written src/neural_operator/fno2d.py
# neuraloperator>=0.3
